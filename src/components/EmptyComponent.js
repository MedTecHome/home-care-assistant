import React from 'react';

function EmptyComponent() {
  return (
    <h2
      style={{
        color: '#ccc',
        textAlign: 'center',
        marginTop: '5%',
      }}
    >
      No hay datos a mostrar
    </h2>
  );
}

export default EmptyComponent;
